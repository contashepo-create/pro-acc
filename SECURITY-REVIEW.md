# تقرير المراجعة الأمنية والمحاسبية — pro-acc

> **تاريخ الفحص:** 2026-08-07
> **النطاق:** فحص جديد وكامل من الصفر لكامل المستودع (٢٣٣ التزاماً، ~٥٣٦٠٠ سطر TypeScript، ١٩٨ مسار API).
> **ملاحظة:** هذا الفحص **لم يعتمد على أي تقرير سابق** (تمت إعادة الفحص بالكامل كما هو مطلوب).
> **حالة المشروع:** بُني بنجاح (`next build` ✅) ونجحت ٨٨ اختبار وحدة من ٦ مجموعات.

---

## ١) جدول حالة المحاور (الأساس للحكم على الجاهزية)

| # | المحور | الحالة | الملخص |
|---|--------|--------|--------|
| ١ | توقيع التوكن (JWT) | ✅ مُعالَج | `src/lib/auth.ts` يتحقق HMAC-SHA256 بـ`timingSafeEqual` ويعيد جلب الدور من DB. حُذف `auth-secure.ts` (توكن بلا توقيع/قابل للتزوير، ٠ استيراد). وُسِم `auth-real-jwt.ts` مهملاً. |
| ٢ | العزل بين الشركات (RLS) | ✅ مُعالَج تطبيقياً | العميل الخادمي يستخدم `service_role` (يُعطّل RLS)، لذا العزل يعتمد على تصفية `company_id` — وهي **موجودة فعلياً** في كل المسارات المالية والتنظيمية (تحقّق IDOR نظيف). لا وصول لـSupabase من الواجهة (٠ ملف) ⇒ RLS غير ذي صلة هنا ولم تُضَف سياسات ميتة. |
| ٣ | حقن SQL | ✅ سليم | استعلامات مُعاملية عبر `pg Pool` (`db.ts`) وعميل Supabase؛ لا تراكيب قالبية خطيرة. |
| ٤ | CSRF | ✅ مُخفَّف | دوال `requireCsrf`/`checkCsrf` موجودة لكن **غير موصولة** (٠ استدعاء)؛ والواجهة **لا تُرسل رمز CSRF** ⇒ تفعيلها كان سيُعطّل التطبيق. الحماية الفعلية هي `SameSite: lax` على الكوكيز (تخفيف صالح لـCSRF العابر للمواقع). |
| ٥ | أمان نقل قاعدة البيانات (TLS) | ⚠️ يُوصى بضبط | `rejectUnauthorized: false` افتراضياً؛ الكود يدعم `DATABASE_CA_CERT`. اضبطه في الإنتاج. |
| ٦ | وسيط مركزي (middleware) | ✅ مقبول | لا يوجد `middleware.ts`؛ التحقق لكل مسار. مسارات `admin` محمية بفحص `superadmin`، والمسارات العامة (تسجيل دخول/مدخل العميل) متعمَدة. |
| ٧ | سلامة المحاسبة (توازن القيد) | ✅ مُعالَج | مسار اليومية يستخدم RPC ذرّي `create_journal_entry` (يحقّق التوازن ذرّياً). وُحُصِّن `createJournalEntry` بفرض `|مدين−دائن| ≤ ٠٫٠١` + تنظيف القيد اليتيم — يحمي ١٣+ مساراً. |
| ٨ | تسلسل الترقيم (race) | ✅ مُعالَج | المسار الآمن عبر دوال RPC ذرّية (`next_invoice_number`, `next_journal_number`, ...)؛ البدائل الاحتياطية سباقة لكنها لا تُستدعى إلا عند غياب RPC. |
| ٩ | المنطق الضريبي (VAT) | ✅ سليم | `vat_amount = subtotal * vat_rate`، `total = subtotal + vat`، وبند الفاتورة `quantity * unit_price` — صحيح. |
| ١٠ | ZATCA (الفوترة الإلكترونية) | ✅ مُختبر | `src/lib/zatca/` موجود ومغطى باختبارات وحدة (`zatca.test.ts` نجح). |
| ١١ | تكرار مجلدات الميجريشنز | ✅ وُوثِّق | `src/migrations` + `run.ts` المصدر الموحّد (idempotent). وُوثّق تحذير `supabase/migrations` المتباعد في `MIGRATIONS.md`. |
| ١٢ | الأسرار المخزّنة في الكود | ✅ نظيف | لا مفاتيح صلبة؛ كلها عبر متغيرات بيئة (التسريب القديم مُزال). |
| ١٣ | الاختبارات والبناء | ✅ نجح | `npm run build` ✅ ؛ ٨٨/٨٨ اختبار وحدة ✅. (`ignoreBuildErrors: true` مُبقًى لتجنّب كسر البناء بأخطاء أنواع سابقة — يُوصى بحلها لاحقاً.) |

---

## ٢) الإصلاحات المطبّقة (مُدفوعة إلى `main` وفروع الجلسة)

| الالتزام | الفرع | المحتوى |
|----------|-------|---------|
| `0bae33c` | `session/remove-forgeable-auth-20260807` | حذف `src/lib/auth-secure.ts` (توكن بلا توقيع قابل للتزوير) + وسْم `auth-real-jwt.ts` مهملاً. |
| `ace1bdd` | `session/accounting-integrity-20260807` | حذف `src/lib/safe-numbering.ts` (كود ميت مكسور) + تحصين `createJournalEntry` بفرض التوازن وتنظيف القيد اليتيم. |
| (هذا الالتزام) | `session/hardening-idor-docs-20260807` | تحصين IDOR دفاعي في `invoices/[id]` (تصفية `company_id` على التحديثات المعتمدة على id) + `MIGRATIONS.md` + هذا التقرير. |

---

## ٣) المخاطر المتبقية والتوصيات

1. **`ignoreBuildErrors: true`** — يُخفي أخطاء الأنواع. يُوصى بحل أخطاء TS وإيقاف التجاوز قبل الإنتاج.
2. **TLS قاعدة البيانات** — اضبط `DATABASE_CA_CERT` لإجبار التحقق من الشهادة في الإنتاج.
3. **تباعد الميجريشنز** — طبّق `src/migrations` كمصدر وحيد عبر `npx tsx src/migrations/run.ts`؛ لا تخلط مع `supabase db push`.
4. **تدقيق خطّي لكل مسار** — تم التحقق من التصفية بـ`company_id` في المسارات المالية والتنظيمية وعيّنات من المسارات التنظيمية الأخرى؛ توصية بالمراجعة الخطّية الكاملة لكل مسار قبل الإطلاق.
5. **اختبارات التكامل** — تتطلب قاعدة بيانات حية؛ لم تُشغَّل في هذه البيئة. يُوصى بتشغيلها في CI ببيئة اختبارية.

## ٤) حدود الفحص
- الفحص تم في بيئة بدون قاعدة بيانات حية أو متغيرات بيئة الإنتاج؛ لذلك:
  - البناء ونجاح اختبارات الوحدة هما الدليل على السلامة البنائية والمنطقية.
  - التحقق من التشغيل الفعلي (تسجيل دخول، إنشاء فاتورة، ترحيل قيد) يتطلب بيئة ببيانات حقيقية.
- هذا التقرير **لا يُغني عن اختبار اختراق مباشر (penetration test)** قبل الإطلاق الإنتاجي.
